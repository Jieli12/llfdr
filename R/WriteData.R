# run CVLC
# usethis::use_data(h1_lc, overwrite = TRUE)

# run CVLL
# usethis::use_data(h1_ll, overwrite = TRUE)

# run YResidualH1LC
# usethis::use_data(Yresid_lc, overwrite = TRUE)

# run YResidualH1LL
# usethis::use_data(Yresid_ll, overwrite = TRUE)

# run CVHstdLC
# usethis::use_data(hstd_lc, overwrite = TRUE)

# run SigmaHstdLC
# usethis::use_data(Sigma_hstd_lc, overwrite = TRUE)
# usethis::use_data(Ystd_LC, overwrite = TRUE)

# run CVHstdLL_LS
# usethis::use_data(hstd_LL_LS, overwrite = TRUE)

# run SigmaHstdLL_LS
# usethis::use_data(Sigma_LL_LS, overwrite = TRUE)
# usethis::use_data(Ystd_LL_LS, overwrite = TRUE)

# run CVHstdLL_LH
# usethis::use_data(hstd_LL_LH, overwrite = TRUE)

# run SigmaHstdLL_LS
# usethis::use_data(Sigma_LL_LH, overwrite = TRUE)
# usethis::use_data(Ystd_LL_LH, overwrite = TRUE)

# run DCMCVh
# usethis::use_data(hdcm1, overwrite = TRUE)

# run DCMCVhMu
# usethis::use_data(hdcm2, overwrite = TRUE)

